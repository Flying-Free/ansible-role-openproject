---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  vars:
    is_postgresql_installed: yes
    is_apache_installed: no
    openproject_postgres:
      host: localhost
      name: localhost
      user: openproject
      pass: changeme
  tasks:
  - name: Example assertion
    assert:
      that: true
